export interface ProductType {
  name: string
  price: number
}

export interface WorkerType {
  name: string
}

export interface UserType {
  user_id: string
  username: string
  password: string
  role: string
}
